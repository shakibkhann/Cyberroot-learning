# CyberRoot

A single-file cybersecurity learning platform (frontend-only MVP demo). No build step, no backend — just index.html.

## Live Demo
After enabling GitHub Pages: https://<your-username>.github.io/cyberroot/

## Notes
All progress (XP, quiz scores, CTF flags) is stored in each visitor's browser via localStorage — no shared database.
